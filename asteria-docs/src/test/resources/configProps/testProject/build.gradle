plugins {
    id "one.leftshift.asteria-docs" apply false
}

group = "com.example"
version = "0.0.1-SNAPSHOT"

allprojects {
    repositories {
        jcenter()
    }
}

subprojects {
    apply plugin: "java"

    dependencies {
        testCompile "junit:junit:4.12"
    }
}
