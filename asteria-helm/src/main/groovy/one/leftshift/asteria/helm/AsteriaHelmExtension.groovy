package one.leftshift.asteria.helm

class AsteriaHelmExtension {

    /**
     * Chart version. If general gradle project version is a snapshot, then the chart version will be a snapshot version as well.
     */
    String chartVersion = "0.1.0"
}
